# Copy to .env and fill in. Never commit .env.

# Required: TypeSafe API key dedicated to Spend Gate (not your personal demo key)
TYPESAFE_API_KEY=

# Optional
TYPESAFE_MODEL=jev-latest
PORT=8787
TASK_FIT_THRESHOLD=0.55
VALUE_THRESHOLD=0.5
